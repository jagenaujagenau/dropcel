name: CI

on:
  push:
    branches: [main]
  pull_request:
  # Called by release.yml so a tag cannot publish without passing CI.
  workflow_call:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
      - run: pnpm install
      - run: npx tsc --noEmit
      - run: npx oxlint
      - run: npx vitest run
      - run: npx vite build
      - run: cargo test
        working-directory: src-tauri
